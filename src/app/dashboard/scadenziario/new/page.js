import { ScadenziarioCreateView } from 'src/sections/scadenziario/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Nuovo scadenziario'
};

export default function ScadenziarioCreatePage() {
  return <ScadenziarioCreateView />;
}
