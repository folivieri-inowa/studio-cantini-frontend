'use client';

import Container from '@mui/material/Container';
import Typography from '@mui/material/Typography';

export default function ArchiveListView() {
  return (
    <Container>
      <Typography variant="h3">Archivio Digitale - Test</Typography>
      <Typography>Se vedi questo messaggio, il routing funziona.</Typography>
    </Container>
  );
}
